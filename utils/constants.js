const buttonSettings = {
  buttonClass: "button",
  activeButtonClass: "button_active",
};

const panelSettings = {
  panelClass: "panel",
  activePanelClass: "panel_active",
};

export {
  buttonSettings,
  panelSettings,
};
